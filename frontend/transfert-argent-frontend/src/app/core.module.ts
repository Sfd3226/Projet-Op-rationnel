import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  imports: [
    CommonModule
  ],
  providers: [
    // La configuration de l'intercepteur a été déplacée vers app.config.ts
  ]
})
export class CoreModule {}